# Technical Debt Backlog

Items found during review and consciously **not** resolved. Every row cites a current `file:line`, states why it was left, and says what fixing it would involve. Rows marked `[x]` were resolved on the branch that found them and are kept for the reasoning, not as outstanding work.

The one genuine correctness defect in shipped behaviour — `sanitize.go:340`, pointer map keys being re-keyed — is RESOLVED as of 2026-09-12 on branch `td/2026-09-12`. A non-string map key is still walked, so errors from inside it propagate, but its rebuilt result is discarded and the caller's own key survives. Everything else open is cost, contract clarity, or test and comment quality.

`.planning/` is gitignored in this repo (`.gitignore:10`), so this file is local-only, exactly like `STATUS.md` and `INTEGRATION.md`.

**Checkbox legend:** `[ ]` open · `[/]` deferred by an explicit decision · `[x]` resolved.

### [2026-09-12] From Review: PR #9 output-path performance, atcr panel pass 1

Panel ran `partial: true` — `bruce`, `mira` and `pace` truncated with zero surviving findings, so coverage was 8 of 11 reviewers.

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|-----------|------------|
| 2 | [ ] | LOW | sanitize.go:156 | `out, _, err := sanitizeValue(rv)` discards the changed flag at the only public boundary. A caller that needs the pre-existing guaranteed-distinct copy therefore has no way to ask for one, short of running its own `reflect.DeepEqual` against the value it just passed in. | Expose the flag, or add a `SanitizeCopy` variant for callers that need isolation, rather than leaving them to infer it. | api-contract | 20 | atcr-panel | brad | MEDIUM |
| 3 | [ ] | LOW | check_test.go:192 | Two 100-iteration loops each call the full `Check` — `Sanitize`, `inspect`, both lossy walks, `toon.Marshal` and `json.Marshal` — so roughly 200 complete encode passes run in order to assert a memoization property. The map-iteration-randomisation argument justifies repeating the case, but not repeating it at the top-level entry point. | Swap both loop bodies to `lossyInValue(reflect.ValueOf(v), map[nodeID]bool{})` and assert on the returned reason string instead of `Check(...).OK`. Set the bound to 20. Marshalling drops out of the test entirely. | testing | 15 | atcr-panel | brad | MEDIUM |
| 3 | [ ] | LOW | output_bench_test.go:68 | `BenchmarkOutput_WritersDirty` measures `Encode` and `EncodeOrJSON` but omits `EncodeChecked`, the writer whose entire justification in the sibling benchmark is guard overhead. The dirty path is precisely where the rebuild interacts with the guard, so the one comparison that would show that interaction never runs. | Add the `EncodeChecked` sub-benchmark, for symmetry with `BenchmarkOutput_Writers`. | testing | 5 | atcr-panel | brad | MEDIUM |
| 3 | [ ] | LOW | sanitize_bench_test.go:92 | `TestSanitize_CleanPayloadDoesNotPayToBeCopied` and `TestSanitize_DirtyPayloadDoesNotPayTwice` (`:252`) both say "Pay" where the measured quantity is allocations. A metaphor sitting where a concrete term exists. | `TestSanitize_CleanPayloadAllocatesNoCopy` and `TestSanitize_DirtyPayloadBuildsOnce`. Mechanical rename; no assertion or threshold changes. | naming | 2 | atcr-panel | otto | MEDIUM |

### [2026-09-12] From Review: PR #9 output-path performance, atcr panel pass 2

Second pass over `db54cc11`, because pass 1 reviewed an earlier commit and never saw the walk merge. Also `partial: true`; 12 of 13 agents succeeded. Seven findings were fixed on the branch and one claim refuted by probe.

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|-----------|------------|
| 5 | [/] | MEDIUM | check.go:460 | The depth cap was removed from all walkers on the argument that termination does not need it. That is sound about termination and silent about stack depth: the seen-set halts loops, but recursion depth still equals value depth, and a stack overflow is fatal and uncatchable. Deferred on measurement rather than dismissed: `encoding/json` rejects beyond 10,000 nesting levels, and a payload dirty at depth 10,000 sanitizes in 14ms without overflowing, so no input arriving through JSON reaches the stack limit. The exposure is a caller hand-building a value deeper than JSON can express. | If it is worth closing, add a depth counter returning an explicit `*DepthError` past a bound matching `encoding/json`'s 10,000 — an error is recoverable where a stack overflow is not. Do not reintroduce a cap that returns a FALSE answer, which is the bug the memoization replaced. | security | 120 | atcr-panel-2 | brad,otto | HIGH |
| 7 | [ ] | LOW | output_test.go:418 | The fixture guard for `TestEncodeOrJSON_DoesNotMeasureSizeOnTheLosslessPath` establishes losslessness by calling `Check`, the function whose walks the test exists to watch. If `isLossyType` ever stops flagging the probe type — it checks `encoding.TextMarshaler` alone — the probe silently becomes lossy, the guard still passes, and the test asserts about the envelope branch while claiming to watch the TOON branch. | Assert on the emitted bytes instead: capture the output and require it not to begin with `{"axi_format`, which is independent of the detector under test. | testing | 25 | atcr-panel-2 | brad | MEDIUM |
| 7 | [ ] | LOW | sanitize.go:199 | `needsCleaning` is dead in production yet its comment calls the property it guards "the load-bearing invariant of the whole copy-on-write scheme". The invariant is enforced by `inspect`'s pointer-threaded flag inside `Sanitize`, and this wrapper does not exercise that path identically — it discards the cycle result, so on a cyclic value it returns false where `Sanitize` refuses. `TestSanitize_PredicateAgreesWithTheWalk` therefore pins a shim, not the production gate. | Delete `needsCleaning` and have the agreement test call `inspect` with a `*bool` directly, matching how `Sanitize` uses it. | maintainability | 30 | atcr-panel-2 | brad | MEDIUM |
| 8 | [ ] | LOW | check.go:471 | `inspect` looks the path map up twice per reference-bearing node — once to test, once to assign. | Use the comma-ok form and assign in the else branch. | style | 2 | atcr-panel-2 | otto | MEDIUM |
| 8 | [ ] | LOW | sanitize.go:332 | The comment "cannot be skipped for a map whose own keys happen to be clean" reads awkwardly. | "cannot be skipped even when this map's own keys are clean". | style | 2 | atcr-panel-2 | otto | MEDIUM |

### [2026-09-13] From Review: PR #10 reflect-free walks, atcr panel

18 findings (0 critical, 2 HIGH, 11 MEDIUM, 5 LOW), `partial: true`, 12 of 13 agents. Both HIGHs and the substantive MEDIUMs were resolved on the branch; two findings were refuted with evidence and four rejected. The panel's single most valuable contribution was proving that a guard I had written was dead.

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|-----------|------------|
| 10 | [ ] | LOW | fastwalk.go:38 | The scalar fast arm omits `complex64`, `complex128` and `uintptr`, and there is no `[]byte` arm. All four fall through to the reflect walk, which is CORRECT but not fast. None appears in a realistic TOON payload, so this is an opportunity rather than a defect. | Add the missing scalar kinds to the existing arm, and a `[]byte` arm if a consumer ever carries one. Measure before adding: each arm costs a type-switch case on every value. | performance | 15 | self-review | self | HIGH |

### [2026-09-12] From Review: PR #9 self-review, hostile pass over the full diff

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|-----------|------------|
| 4 | [ ] | LOW | check_test.go:106 | The comment reads "60 layers clears a cap of 100 with margin", mixing layer-units with walk-depth units — each `[]any` layer costs two levels of walk depth. The claim is true but its arithmetic is invisible, and the probe table immediately above gives the real boundary at 50 layers. | State it in one unit: 60 layers is 120 levels of walk depth, which clears the old cap of 100. | docs | 5 | self-review | self | HIGH |
| 4 | [ ] | LOW | output_test.go:558 | `losslessRows` duplicates `benchRows(n, false)` at `sanitize_bench_test.go:21`. Two near-identical uniform row fixtures now exist in the same package, differing only in that `losslessRows` adds the scalar `path` and `total` keys a real listing carries. | Keep one. `benchRows` already takes a dirty flag, so extend it with the scalar keys and delete `losslessRows`, or move the other way — but not both. | duplication | 15 | self-review | self | HIGH |

## Stats

| Severity | Open | Deferred | Resolved |
|----------|------|----------|----------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 0 | 0 | 0 |
| MEDIUM | 2 | 1 | 0 |
| LOW | 13 | 0 | 2 |

**Last Modified:** 2026-09-13 | **Open Items:** 15 | **Deferred Items:** 1 | **Resolved Items:** 2 | **Total Items:** 18

Counted with `awk`, not by hand. Three separate attempts to tally this table by eye produced wrong numbers — 6 versus 5 open MEDIUM the first time, then 11/5/17 against the real 13/4/18 — so the count is now always taken from the file rather than from memory of what was added.

### [2026-09-13] From Review: perf-review-benchmarks branch, atcr panel + own pass

Branch `perf-review-benchmarks` (`b58bd16`, local only, never pushed). 389 lines across `check.go`, `fastwalk.go`, `output.go`, `sanitize.go`, with **no test changes**. atcr returned 25 findings reconciled to 14 (0 critical, 5 HIGH, 7 MEDIUM, 2 LOW), `partial: true`, 13 of 14 agents; `mira` truncated with zero surviving findings.

The speed work is real and measured over 6 runs per benchmark: `SanitizeString` clean-long -64.9%, `Sanitize` clean 500 rows -34.4%, `EncodeOrJSON` -7.8%, `Check` -9.4%. Nothing regressed. A differential probe over 200,000 random inputs proved the new `cleanString` ASCII table byte-identical to the implementation it replaced, and the `asciiSafe` table an exact match for `unsafeRune` on all 256 bytes.

Three findings were held back as BLOCKERS rather than filed here: `Sanitize` losing its entire godoc (60 lines, including the concurrency hazard) to a misplaced `var pathPool`; `isTabularHeader` diverging from the regex it replaced, proven by probe on `"rows[2\nx]{a}:"`; and the absence of any test for two hand-rolled reimplementations. Two atcr findings were refuted against the code: `check.go:248` is not a leak — an oversized map is deliberately dropped rather than pooled, and the proposed "always Put" fix would park a multi-megabyte bucket array in the pool — and otto's "missing Put on the happy path" at `sanitize.go:122` is contradicted by the `pathPool.Put(path)` on the line after the cycle branch.

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|-----------|------------|
| 11 | [ ] | MEDIUM | fastwalk.go:76 | Replacing `defer delete(path, id)` with a bare `delete` after the loop means the cycle-detected unwind (`if fastInspect(val, path, dirty) { return true }`) no longer pops this frame's id, so the path map is left populated on exactly the path where it is recycled. This is correct TODAY only because `Sanitize` calls `clear(path)` on its cycle branch, and that correctness now spans two files and two walkers with different pop semantics — `inspect` still pops via `defer` on every exit. The file's own header comment still claims "the same pop-on-exit" as an invariant shared with `inspect`, which is now false. Any future early return added between `path[id] = true` and the delete parks stale ids in a POOLED map, and the next `Sanitize` drawing that map reports a false `*CycleError` on an acyclic payload — which `MustSanitize` turns into a panic. | Restore the `defer`-based pop, or delete before each `return true`, and correct the invariant comment to state the actual semantics. Pin it with a test asserting the path map is empty after a walk that DID find a cycle, not only after a clean one. | invariant | 15 | atcr-panel | brad,kai | HIGH |
| 11 | [ ] | MEDIUM | sanitize.go:145 | The pooled path map has no size bound before it returns to `pathPool`, and `clear()` does not shrink a Go map's bucket array. `verdictFor` guards this exact case with `if len(nodeSeen) <= maxLossyMemo`; `Sanitize` does not. One hostile wide payload with millions of map and slice nodes grows the map to a large bucket array that is then parked in the pool and handed out again, inflating the steady-state footprint of every later `Sanitize` on that P until a GC drains the pool. | Drop the map instead of pooling it when `len(path)` exceeds a bound, mirroring the `maxLossyMemo` check in `verdictFor`. | resource-leak | 10 | atcr-panel | brad | HIGH |
| 12 | [ ] | LOW | output.go:196 | `writeLine` re-pools only when `cap(buf) <= 65536`, and when `cap(buf) < len(b)+1` it allocates a fresh buffer while `*p` still points at the old one, so the old buffer is dropped without being returned. A stream of lines just over 64KB therefore allocates `len(b)+1` on every call AND discards the pooled 4KB buffer without replacing it — the pool gives zero reuse on exactly the large-payload path it was added for. The 2x copy of the body is also retained. | Return the old buffer to the pool when replacing it, or use size-classed pools, and state the retention policy in a comment mirroring the bounded-map policy used elsewhere in this change. | performance | 15 | atcr-panel | brad,kai | HIGH |
| 12 | [ ] | LOW | output.go:178 | `var lineBufPool` was inserted directly after `writeLine`'s doc comment with no blank line, so the "single Write call … no half-written state for a consumer to misread" rationale now documents the pool and `writeLine` is left undocumented. Same defect class as the `Sanitize` blocker, but on an unexported function, so it costs readability rather than public API documentation. | Insert a blank line before `var lineBufPool`, or hoist the var above the comment block. | docs | 5 | atcr-panel | brad,otto | HIGH |
| 12 | [x] | LOW | check.go:110 | RESOLVED on the branch: the regexp is live again as the single definition of a tabular header, with `isTabularHeader` reduced to deciding when running it can be skipped. Original finding: `tabularHeader` is now dead: `isTabularHeader` replaced its only call site, but the `regexp.MustCompile` var remains, still compiled at init, as a competing source of truth for what a tabular header is. Nothing flags it — Go does not error on an unused package-level var and `golangci-lint` is configured but never runs in CI. | Keep it deliberately as the oracle for the equivalence test the blocker requires, and say so in its comment; otherwise delete it. Do not leave it undeclared either way. | maintainability | 10 | self-review | self | HIGH |
| 12 | [x] | LOW | check.go:114 | RESOLVED on the branch: the hand-rolled parser was deleted in favour of a two-byte pre-check plus the regexp, which is provably equivalent and measured 92x faster than the parser on the list shape that motivated it. Original finding: `isTabularHeader` is a 5-level nested state machine over single-letter variables, replacing a one-line regex. Reviewable only by careful trace, which is how the equivalence divergence survived to review. | Extract a helper per parsing stage, or keep the nesting and buy the safety back with the differential test the blocker requires. | maintainability | 15 | atcr-panel | otto | MEDIUM |
